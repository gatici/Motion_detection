# Image size
img_size = (640, 360)

# GaussianBlur kernel size
blur_kernel = (3, 3)

# Threshold value and max value for cv2.threshold function
trash_val = 25
max_val = 255

# Minimum area for contours
min_area = 80

# Rectangle features to draw
rect_color = (0, 0, 255)
rect_line = 2

